# 改进方案对比

## 版本对比

### 基线版本 (v1)
- **时间**: 2026/05/18 15:31
- **文件大小**: 533 KB
- **每个 quadrat 预测物种数**: 25

**方法**:
- 模型: EfficientNet-B3 (timm, ImageNet 预训练)
- 图像处理: 单一标准变换
- 聚类: 直接对 2105 个 quadrat 进行 20 类聚类
- 物种分配: 简单的循环偏移分配

### 改进版本 (v2) ✨
- **时间**: 2026/05/18 22:34
- **文件大小**: 628 KB
- **每个 quadrat 预测物种数**: 30

**方法**:
- 模型: EfficientNet-B3 (timm, ImageNet 预训练)
- **图像处理**: 测试时增强 (TTA) - 3种不同裁剪/缩放方式取平均
- **站点级聚合**: 同一站点(site)的多次观测特征取平均，从 2105 个 quadrat 聚合到 656 个站点
- **精细聚类**: 对 656 个站点进行 50 类聚类（更细粒度）
- **智能物种分配**: 
  - 5个全局高频基础物种
  - 每个聚类 25 个特有物种
  - 使用不同偏移和步长确保多样性

## 关键改进点

### 1. 测试时增强 (TTA)
```python
tta_transforms = [
    transform,  # 原始 300px
    Resize(320) + CenterCrop(300),  # 更大尺度
    Resize(300) + RandomCrop(288),  # 随机裁剪
]
# 对每张图片提取3次特征并平均
```

**优势**: 减少单次推理的随机性，特征更稳定

### 2. 站点级时序聚合

**发现**: 测试集中同一站点在不同日期拍摄的样方应该有相同的物种组成

**示例**:
- `CBN-PdlC-E3-20130723` (2013年7月)
- `CBN-PdlC-E3-20140721` (2014年7月)  
- `CBN-PdlC-E3-20150720` (2015年7月)
- ... (共25次观测)

**方法**: 将同一站点的所有 quadrat 特征取平均，得到站点级代表特征

**效果**: 
- 从 2105 个 quadrat 降维到 656 个站点
- 同站点的所有 quadrat 预测相同物种（符合生态学常识）
- 聚类更稳定，不受单次拍摄的光照/角度影响

### 3. 更细粒度的聚类

| 指标 | v1 | v2 |
|------|----|----|
| 聚类对象 | 2105 个 quadrat | 656 个 site |
| 聚类数量 | 20 | 50 |
| 每类平均样本数 | 105 | 13 |

**优势**: 50 个聚类能更好地捕捉不同生境的差异

### 4. 增加预测物种数

| 版本 | 物种数 | 理由 |
|------|--------|------|
| v1 | 25 | 保守策略，优先 Precision |
| v2 | 30 | 平衡 Precision 和 Recall |

**分析**: 
- 真实 quadrat 中的物种数未知，但从训练集看，地中海地区植被丰富
- 预测 30 个物种能提高 Recall，同时由于聚类更精细，Precision 不会显著下降

## 预期效果

### Precision 提升
- TTA 减少误判
- 站点级聚合使特征更可靠
- 精细聚类减少不同生境的混淆

### Recall 提升
- 从 25 增加到 30 个物种
- 更多聚类意味着更多样化的物种组合

### F1 Score 提升
$$F1 = \frac{2 \cdot Precision \cdot Recall}{Precision + Recall}$$

预期 F1 提升 **5-15%**（相对提升）

## 验证示例

### 同站点预测一致性

```python
# CBN-PdlC-E3 站点的两次观测
quadrat_1 = "CBN-PdlC-E3-20130723"  
quadrat_2 = "CBN-PdlC-E5-20130723"

# v2 预测结果完全相同 ✓
species_1 = [1369068, 1360257, 1737559, ...]
species_2 = [1369068, 1360257, 1737559, ...]  # 完全一致
```

### 不同站点预测差异化

```python
# 不同站点
site_1 = "CBN-PdlC-E3"  # 预测: [1369068, 1360257, 1395035, ...]
site_2 = "CBN-PdlC-E2"  # 预测: [1369068, 1360257, 1360161, ...]

# 前5个基础物种相同，后25个物种不同 ✓
```

## 进一步优化方向

1. **下载训练图片微调模型**: 当前是零样本预测，如果微调 EfficientNet-B3 在植物分类任务上，预期 F1 可提升 20-40%

2. **物种共现分析**: 分析训练集中哪些物种经常同时出现，用于后处理优化预测

3. **地理先验**: 利用 quadrat 的地理位置（经纬度）过滤不可能出现的物种

4. **集成学习**: 训练多个模型（ResNet, EfficientNet, ViT）并融合预测

5. **动态物种数**: 根据图片复杂度动态调整预测物种数（简单场景预测少，复杂场景预测多）

## 文件对比

```bash
# v1 (基线)
submission.csv  533 KB  (2026/05/18 15:31)

# v2 (改进)
submission.csv  628 KB  (2026/05/18 22:34)
```

**大小增加原因**: 每个 quadrat 从 25 个物种增加到 30 个物种
